-- import as flat file -> 
-- submission id:NUMBER
-- reason: VARCHAR(128)

SELECT * FROM CATEGORIZED_DENIAL_REASONS;

SELECT REASON, COUNT(REASON) AS "Reason Count"
FROM CATEGORIZED_DENIAL_REASONS
GROUP BY REASON;
